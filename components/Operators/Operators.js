import styles from './Operators.module.scss';

const Operators = ({
    operators
}) => {
    console.log('Operators: ', operators);
    return (
        <div>
            Operators
        </div>
    );
}

export default Operators;
