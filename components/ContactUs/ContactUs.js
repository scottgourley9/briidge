import styles from './ContactUs.module.scss';

const ContactUs = () => {

    return (
        <section className={styles['contact-us-page-wrapper']}>Contact</section>
    );
}

export default ContactUs;
